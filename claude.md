# Claude.md

This repo contains multiple projects for XRPL Commons.

## Repo structure

- `xrpl.js` contains different projects with typescript code using the xrpl.js SDK.
- `mpt-demo` is a React UI
- `batch` is a React UI
